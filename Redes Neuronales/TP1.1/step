function step = transfer(number)
	step = [];
	for i = number'
		if(i>0)
			step = [step; 1];
		else
			step = [step; 0];
		endif
	endfor
endfunction

function step = correction(guess)
	step = ones(length(guess),1);
endfunction
