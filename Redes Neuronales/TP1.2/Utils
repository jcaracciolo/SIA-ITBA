function answer = step(n)
	if(n>=0)
		answer = 1;
	else
		answer = 0;
	endif
endfunction

function answer = stepEpsilon(n)
	global validateEpsilon
	if (n>=(1-validateEpsilon))
		answer = 1;
	elseif (n<=(-1+validateEpsilon))
		answer = -1;
	else
		answer = n;
	endif
endfunction
